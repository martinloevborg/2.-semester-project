import presentation.GUI.App;

/**
 * Main entry point of the application.
 */
public class Program
{
    /**
     * Main entry point of the application
     * @param args the command line arguments or switches.
     */
    public static void main(String[] args) {
//        new Main().load(args);
        new App().load(args);
    }
}
